{
  "name": "ledgerlens",
  "private": true,
  "version": "0.1.0",
  "description": "LedgerLens — Groq Vision Doc QA. Upload invoice photo, get bbox-grounded answers.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:web": "bun run --filter web dev",
    "dev:api": "bun run --filter api dev",
    "dev:worker": "uv run --project services/vision-worker fastapi dev app/main.py",
    "build": "bun run --filter web build",
    "lint": "biome check . && bun run --filter '*' lint",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test && uv run --project services/vision-worker pytest",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.4.0"
}
